# image-processing-library
Custom image processing library in C++ implementing pixel-level arithmetic operations (addition, subtraction, dot product, scaling) and image resizing using an inheritance-based Matrix/Image class hierarchy
